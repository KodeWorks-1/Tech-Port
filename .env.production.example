# Copy to .env.production on the server. Never commit the real file.
ENV=production
PORT=8080
DATABASE_URL=postgres://techport:CHANGE_ME@db:5432/techport?sslmode=disable
# IMPORTANT: demo mode defaults ON (no admin login!). Must be 0 in production.
DEMO_MODE=0
