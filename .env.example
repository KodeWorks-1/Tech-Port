# Copy to .env for local development
ENV=dev
PORT=8080
DATABASE_URL=postgres://techport:techport@localhost:5543/techport?sslmode=disable
# Demo mode is ON by default: /admin needs no login, every visitor is
# auto-logged-in as the demo customer, logout/signup are hidden.
# DEMO_MODE=0 restores real auth.
#DEMO_MODE=1
